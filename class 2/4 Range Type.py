#It's main purpose is to generate number data
#numbers=range(starting point,End point,How much jump)
numbers = range(0,100,3)
myList=list(numbers)
#we can convert it into tuple then access it by their index number
myTuple = tuple(numbers)
print(myTuple)
print(myList)